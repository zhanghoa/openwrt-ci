name: IPQ60XX-6.12-NOWIFI-NSS

on:
  workflow_dispatch:
    inputs:
      config_preset:
        description: '选择预设配置（仓库 configs/ 目录下的文件）'
        required: true
        default: 'ipq60xx-6.12-nowifi-nss.config'
        type: choice
        options:
          - ipq60xx-6.12-nowifi-nss.config
          - ipq60xx-6.12-nowifi-nss-full.config
          - ipq60xx-6.12-nowifi-nss-mini.config
          - custom (手动输入文件名)
      custom_config_name:
        description: '当选择 "custom" 时，请输入配置文件路径（如 configs/my.config）'
        required: false
        type: string
      ssh:
        description: '启用 SSH 调试（在 feeds 安装后，编译前进入）'
        required: false
        default: 'false'
        type: boolean
      upload_config:
        description: '上传生成的 .config 文件到 Artifact'
        required: false
        default: 'false'
        type: boolean
      skip_compile:
        description: '仅生成配置并上传，跳过实际编译（配合 upload_config 使用）'
        required: false
        default: 'false'
        type: boolean

env:
  REPO_URL: https://github.com/LiBwrt/openwrt-6.x.git
  REPO_BRANCH: main-nss
  CONFIG_FILE: configs/ipq60xx-6.12-nowifi-nss.config
  DIY_SCRIPT: libwrt.sh
  CLASH_KERNEL: amd64
  CACHE_TOOLCHAIN: true
  UPLOAD_BIN_DIR: false
  FIRMWARE_RELEASE: true
  FIRMWARE_TAG: IPQ60XX-6.12-NOWIFI-NSS
  TZ: Asia/Shanghai

jobs:
  Build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write

    timeout-minutes: 360

    steps:
    - name: 快速清理磁盘空间
      run: |
        echo "清理大型工具以腾出空间..."
        sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
        df -h

    - name: 显示系统信息
      run: |
        echo "=== CPU ==="; lscpu | grep "Model name"
        echo "=== Memory ==="; free -h
        echo "=== Disk (清理后) ==="; df -h

    - name: 安装编译依赖
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential ccache ecj fastjar file g++ gawk \
          gettext git java-propose-classpath libelf-dev libncurses5-dev libncursesw5-dev \
          libssl-dev python3-dev python3-pip rsync subversion sudo unzip wget \
           xz-utils zlib1g-dev dos2unix libfuse-dev
        sudo timedatectl set-timezone "$TZ"

    - name: 配置 ccache
      run: |
        mkdir -p ~/.ccache
        echo "max_size = 10.0G" > ~/.ccache/ccache.conf

    - name: 缓存 ccache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ${{ runner.os }}-ccache-${{ hashFiles(env.CONFIG_FILE, env.DIY_SCRIPT) }}
        restore-keys: |
          ${{ runner.os }}-ccache-

    - name: 合并磁盘（可选增加交换空间）
      uses: easimon/maximize-build-space@master
      with:
        swap-size-mb: 1024
        temp-reserve-mb: 100
        root-reserve-mb: 1024

    - name: 检出仓库
      uses: actions/checkout@v4

    - name: 克隆源代码
      run: |
        git clone --depth 1 -b $REPO_BRANCH $REPO_URL openwrt
        echo "OPENWRT_PATH=$PWD/openwrt" >> $GITHUB_ENV
        cd openwrt
        VERSION_INFO=$(git show -s --date=short --format="作者: %an<br/>时间: %cd<br/>内容: %s<br/>hash: %H")
        echo "VERSION_INFO=$VERSION_INFO" >> $GITHUB_ENV
        VERSION_KERNEL=$(grep -oP 'LINUX_KERNEL_HASH-\K[0-9]+\.[0-9]+\.[0-9]+' target/linux/generic/kernel-6.12 || echo "未知")
        echo "VERSION_KERNEL=$VERSION_KERNEL" >> $GITHUB_ENV

    - name: 缓存 dl 目录
      uses: actions/cache@v4
      with:
        path: openwrt/dl
        key: ${{ runner.os }}-ipq60xx-dl-${{ hashFiles(env.CONFIG_FILE) }}
        restore-keys: |
          ${{ runner.os }}-ipq60xx-dl-

    - name: 生成变量（根据预设选择配置文件）
      id: vars
      run: |
        if [ -n "${{ github.event.inputs.config_preset }}" ]; then
          if [ "${{ github.event.inputs.config_preset }}" == "custom" ]; then
            LOCAL_CONFIG="${{ github.event.inputs.custom_config_name }}"
          else
            LOCAL_CONFIG="configs/${{ github.event.inputs.config_preset }}"
          fi
          echo "CONFIG_FILE=$LOCAL_CONFIG" >> $GITHUB_ENV
          echo "使用手动选择的配置文件: $LOCAL_CONFIG"
        else
          echo "保持默认配置文件: $CONFIG_FILE"
        fi

        # 提前初始化时间变量，防止跳过编译时丢日期
        echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
        echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV
        echo "TAG_TIME=$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_ENV

        cd openwrt
        SOURCE_REPO="$(echo $REPO_URL | awk -F '/' '{print $(NF)}')"
        echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV

    - name: 更新并安装 feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    # ---- SSH 调试 ----
    - name: 设置 SSH 调试（可选）
      if: github.event.inputs.ssh == 'true'
      uses: mxschmitt/action-tmate@v3

    # 应用配置和 DIY 脚本（无论编不编译，这一步必须执行以生成 .config）
    - name: 应用自定义配置并 DIY
      run: |
        cd openwrt
        
        # 复制自定义文件
        if [ -d "$GITHUB_WORKSPACE/files" ]; then
          cp -r $GITHUB_WORKSPACE/files .
        fi
        
        # 复制配置文件
        if [ ! -f "$CONFIG_FILE" ]; then
          echo "::error::找不到配置文件: $CONFIG_FILE"
          exit 1
        fi
        cp $CONFIG_FILE .config
        
        # 运行 DIY 脚本（如果有）
        if [ -f "$GITHUB_WORKSPACE/$DIY_SCRIPT" ]; then
          chmod +x $GITHUB_WORKSPACE/$DIY_SCRIPT
          $GITHUB_WORKSPACE/$DIY_SCRIPT
        fi
        
        # 启用 ccache
        echo "CONFIG_CCACHE=y" >> .config
        
        # 推断目标子目录与安全的 Artifact 命名标识
        DEVICE_LINE=$(grep '^CONFIG_TARGET_.*_DEVICE_.*=y' .config || true)
        if [ -n "$DEVICE_LINE" ]; then
          TARGET_STR=$(echo "$DEVICE_LINE" | sed -r 's/CONFIG_TARGET_(.*)_DEVICE_.*/\1/')
          TARGET_SUBDIR=$(echo "$TARGET_STR" | tr '_' '/')
          TARGET_NAME_STR=$(echo "$TARGET_STR" | tr '_' '-')
          echo "DEVICE_SUBDIR=$TARGET_SUBDIR" >> $GITHUB_ENV
          echo "DEVICE_NAME_STR=$TARGET_NAME_STR" >> $GITHUB_ENV
        else
          if grep -q '^CONFIG_TARGET_ipq60xx=y' .config; then
            echo "DEVICE_SUBDIR=ipq60xx/generic" >> $GITHUB_ENV
            echo "DEVICE_NAME_STR=ipq60xx-generic" >> $GITHUB_ENV
          else
            echo "::warning::无法推断目标子目录，将使用默认 ipq60xx/generic"
            echo "DEVICE_SUBDIR=ipq60xx/generic" >> $GITHUB_ENV
            echo "DEVICE_NAME_STR=ipq60xx-generic" >> $GITHUB_ENV
          fi
        fi

    # 展开依赖关系（无论编不编译，必须跑 make oldconfig 补全依赖）
    - name: 整理并补全 .config 依赖
      run: |
        cd openwrt
        if [ ! -f .config ]; then
          echo "::error::.config 不存在！"
          exit 1
        fi
        yes "" | make oldconfig

    # 仅在需要编译时才去耗费时间下载源码包
    - name: 下载软件包源码
      if: github.event.inputs.skip_compile != 'true'
      run: |
        cd openwrt
        make download -j8
        find dl -name "*.badcheck" -delete

    - name: 验证 .config
      run: |
        cd openwrt
        if ! grep -q '^CONFIG_TARGET_.*_DEVICE_.*=y' .config; then
          echo "::error::设备配置异常，请检查配置文件兼容性。"
          echo "--- 当前 .config 关键差异 ---"
          diff $GITHUB_WORKSPACE/$CONFIG_FILE .config || true
          exit 1
        fi

    - name: 编译固件
      if: github.event.inputs.skip_compile != 'true'
      id: compile
      run: |
        cd openwrt
        echo "使用 $(nproc) 线程编译..."
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 检查磁盘使用
      if: (!cancelled())
      run: df -hT

    # ---- 上传 .config ----
    - name: 上传 .config 文件
      if: github.event.inputs.upload_config == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.SOURCE_REPO }}-config-${{ env.FILE_DATE }}
        path: openwrt/.config
        include-hidden-files: true

    - name: 上传 bin 目录（可选）
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.SOURCE_REPO }}-bin-${{ env.FILE_DATE }}
        path: openwrt/bin

    - name: 整理固件文件
      if: steps.compile.outputs.status == 'success'
      run: |
        cd openwrt/bin/targets/*/*
        cat sha256sums
        cp $OPENWRT_PATH/.config build.config
        mv -f $OPENWRT_PATH/bin/packages/*/*/*.ipk packages 2>/dev/null || true
        tar -czf Packages.tar.gz packages 2>/dev/null || true
        rm -rf packages feeds.buildinfo version.buildinfo 2>/dev/null || true
        echo "FIRMWARE_PATH=$PWD" >> $GITHUB_ENV

    - name: 上传固件到 Artifact（如果未开启 Release）
      if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE != 'true'
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.SOURCE_REPO }}-firmware-${{ env.DEVICE_NAME_STR }}-${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE_PATH }}

    - name: 上传固件到 Release
      if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE == 'true'
      uses: ncipollo/release-action@v1
      with:
        name: R${{ env.DATE }} for ${{ env.FIRMWARE_TAG }}
        allowUpdates: true
        tag: ${{ env.FIRMWARE_TAG }}
        token: ${{ secrets.GITHUB_TOKEN }}
        artifacts: ${{ env.FIRMWARE_PATH }}/*
        body: |
          **This is OpenWrt Firmware for ${{ env.FIRMWARE_TAG }}**
          ### 📒 固件信息(无WIFI)
          - 无WIFI带有线NSS的6.12内核固件
          - 💻 这是 ${{ env.FIRMWARE_TAG }} 平台使用的 OpenWrt 固件
          - ⚽ 固件源码: ${{ env.REPO_URL }}
          - 💝 源码分支: ${{ env.REPO_BRANCH }}
          - 🌐 默认地址: 192.168.1.1
          - 🔑 默认密码: password
          ### 🧊 固件版本
          - 固件内核版本：${{ env.VERSION_KERNEL }}
          - 固件编译前最后一次➦[主源码](${{ env.REPO_URL }})更新记录
          - ${{ env.VERSION_INFO }}
